import { JetBrains_Mono } from 'next/font/google'

const FontMonospace = JetBrains_Mono({
  weight: ['700', '400'],
  display: 'swap',
  variable: '--font-monospace',
  subsets: ['latin'],
})

export default FontMonospace
